<p id="notice"><%= notice %></p>

<% unless @article.photo.blank? %>
  <%= cl_image_tag(@article.photo, height: 300, width: 400) %>
<% end %>


<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', root_path %>
